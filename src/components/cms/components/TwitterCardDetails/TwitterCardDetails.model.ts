import { contentType } from '@optimizely/cms-sdk';

export const TwitterCardDetailsContentType = contentType({
  key: 'TwitterCardDetails',
  displayName: 'Twitter Card Details',
  baseType: '_component',
});
